import React from 'react';
import Modal from '../../src/components/Modal';

export default {
    component: Modal,
    title: 'Modal'
};

export const Default = () => {
    return (
      <Modal />
    );
};
