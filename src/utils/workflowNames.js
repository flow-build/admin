/**
 * Use this object to set all your workflow process names and use it all along your application
 * Keep the pattern [key: value] to an easy understanding, the @param below is used only to exemplify how thing should work
 *
 * @param key A key to identify your workflow process names
 * @param name Your workflows process name
 *
 * @return an Object containing all workflow names
 */
export default {
  key: 'name',
};
